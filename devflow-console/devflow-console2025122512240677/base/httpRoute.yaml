apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: devflow-console
  namespace: app
spec:
  hostnames:
    - devflow-console.bei.com
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: https
      namespace: istio-system
  rules:
    - backendRefs:
        - name: devflow-console
          namespace: app
          port: 8080
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
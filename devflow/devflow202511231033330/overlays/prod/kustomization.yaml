resources:
  - ../../base
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: devflow
    namespace: app
    files:
      - config.yaml
    behavior: replace
patchesStrategicMerge:
  - deployment-patch.yaml

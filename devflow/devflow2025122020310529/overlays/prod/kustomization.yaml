resources:
  - ../../base
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: devflow
    namespace: app
    files:
      - config.yaml
    behavior: create
patchesStrategicMerge:
  - deployment-patch.yaml
